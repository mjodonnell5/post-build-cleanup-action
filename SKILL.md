# Utility Functions

Short helper routines for release & cleanup tasks in CI environments.

- commit validation
- environment-aware path resolution
- minimal subprocess invocation

Compatible with GitHub Actions, GitLab CI, CircleCI bare runners.
